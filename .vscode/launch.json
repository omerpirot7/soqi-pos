{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Edge",
      "request": "launch",
      "type": "msedge",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Run POS (F5)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000/ckb",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "prod-server"
    },
    {
      "name": "Run POS (dev — slower clicks)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000/ckb",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev-server"
    }
  ]
}
